artifact_plexi_research = {
	resources = {
		category = artifact_actions
		cost = {
			minor_artifacts = 750
		}
	}

	ai_weight = {
		weight = 0
	}

	potential = {
		 has_country_flag = plexi_world_found
		 NOT = { has_country_flag = plexian_research_started }
	}

	effect = {
		enable_special_project = {
			name = PLEXIAN_SECRETS
			location = capital_scope
			owner = ROOT
		}

		hidden_effect = {
			set_country_flag = plexian_research_started
			country_event = { id = ancrel.10040 }
		}
	}
}
artifact_lartia_research = {
    resources = {
        category = artifact_actions
        cost = {
            minor_artifacts = 750
        }
    }

    ai_weight = {
        weight = 0
    }

    potential = {
        has_country_flag = lartia_found
        NOT = { has_country_flag = lartian_research_started }
    }

    effect = {
        enable_special_project = {
            name = LARTIAN_SECRETS
            location = capital_scope
            owner = ROOT
        }

        hidden_effect = {
            set_country_flag = lartian_research_started
            country_event = { id = ancrel.10040 }
        }
    }
}
